This program will count numbers
let userNum = Number(prompt("enter your last number"));

let num = 0; //initial variable
while (num <= userNumb) { //condition to test for 
alert(num);
if (num == 6) { alert("you are half way there")
}
num = num + 1; // incrementing statment

}